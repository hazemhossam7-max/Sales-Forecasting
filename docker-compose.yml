version: '3.8'

services:
  sales-forecasting:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./models:/app/models
      - ./mlruns:/app/mlruns
    environment:
      - MLFLOW_TRACKING_URI=file:///app/mlruns
    restart: unless-stopped

